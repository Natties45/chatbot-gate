FROM node:22-slim

RUN apt-get update \
  && apt-get install -y --no-install-recommends git docker.io ca-certificates \
  && rm -rf /var/lib/apt/lists/*

WORKDIR /app
COPY mcp/deploy-agent/deploy-agent.mjs ./deploy-agent.mjs

EXPOSE 4105
CMD ["node", "deploy-agent.mjs"]
